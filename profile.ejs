
<%- include('../partials/header.ejs')%>
    <h2>Welcome, <%= user.firstName%></h2>
    <%if(stories.length){%>
        <p>You have not created stories</p>
        <%stories.forEach(story=>{%>
            <a href='/stories/<%=story.id%>'><p><%=story.title%></p></a>
            <% });
    }else{%>
        <p>No created stories</p>
        <%}%>
    </div>
    <%- include('../partials/footer.ejs')%>
